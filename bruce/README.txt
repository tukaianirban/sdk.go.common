Use this config utility for legacy monolithic deployments where:
- the instance cannot / should not be restarted to deploy a new build
- the instance provides the possibility to inject a build binary into it, while being able to retain the config file in its entirety. 

